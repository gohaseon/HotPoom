<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="messages">

	<select id="selectTitle" parameterType="int">
		SELECT m.no no, content, regdate
		FROM(SELECT MAX(no) no
		 	 FROM messages
	 		 WHERE room_no = 1) r, messages m
		WHERE r.no = m.no
	</select>

</mapper>