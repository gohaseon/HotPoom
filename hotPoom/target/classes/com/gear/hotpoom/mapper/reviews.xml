<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviews">

	<select id="selectReviewListByPoom" parameterType="int" resultType="Review">
		SELECT r.no, r.user_no userNo, r.booking_no bookingNo, r.content, r.score, r.regdate, u.profile_img profileImg
		FROM reviews r, bookings b, pooms p, users u
		WHERE p.no = b.poom_no
		AND r.booking_no = b.no
		AND r.user_no = u.no
		AND poom_no = 19
		ORDER BY r.regdate DESC
	</select>

</mapper>